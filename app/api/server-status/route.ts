import {NextResponse} from "next/server"; import {siteConfig} from "@/lib/config"; export async function GET(){return NextResponse.json({status:"ONLINE",players:42,maxPlayers:100,ping:58,ip:siteConfig.ip,port:siteConfig.port,source:"mock"})}
