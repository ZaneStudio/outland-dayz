import { NextRequest, NextResponse } from "next/server"; import { currentAdmin } from "@/lib/admin"; import { deleteManagedNews, updateManagedNews } from "@/lib/news-store";
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){if(!await currentAdmin())return NextResponse.json({error:"Недостатньо прав"},{status:403});const {id}=await params;const item=await updateManagedNews(id,await request.json());return item?NextResponse.json(item):NextResponse.json({error:"Не знайдено"},{status:404});}
export async function DELETE(_:NextRequest,{params}:{params:Promise<{id:string}>}){if(!await currentAdmin())return NextResponse.json({error:"Недостатньо прав"},{status:403});const {id}=await params;return(await deleteManagedNews(id))?new NextResponse(null,{status:204}):NextResponse.json({error:"Не знайдено"},{status:404});}
