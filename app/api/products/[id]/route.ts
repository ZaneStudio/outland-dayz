import { NextRequest, NextResponse } from "next/server"; import { currentAdmin } from "@/lib/admin"; import { deleteManagedProduct, updateManagedProduct } from "@/lib/product-store";
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){if(!await currentAdmin())return NextResponse.json({error:"Недостатньо прав"},{status:403});const {id}=await params;const body=await request.json();const item=await updateManagedProduct(id,{...body,price:body.price===undefined?undefined:Number(body.price)});return item?NextResponse.json(item):NextResponse.json({error:"Не знайдено"},{status:404});}
export async function DELETE(_:NextRequest,{params}:{params:Promise<{id:string}>}){if(!await currentAdmin())return NextResponse.json({error:"Недостатньо прав"},{status:403});const {id}=await params;return (await deleteManagedProduct(id))?new NextResponse(null,{status:204}):NextResponse.json({error:"Не знайдено"},{status:404});}
