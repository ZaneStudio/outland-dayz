"use client";
import { ImagePlus, Pencil, Plus, Save, ShieldAlert, Trash2, Upload } from "lucide-react"; import { FormEvent, useEffect, useState } from "react"; import type { ManagedProduct } from "@/lib/product-store";
const blank={name:"",description:"",category:"Інше",price:"",image:""};
export default function Admin(){const [products,setProducts]=useState<ManagedProduct[]>([]),[allowed,setAllowed]=useState<boolean|null>(null),[form,setForm]=useState(blank),[editing,setEditing]=useState<string|null>(null),[message,setMessage]=useState("");const load=()=>fetch('/api/products').then(r=>r.json()).then(setProducts);useEffect(()=>{fetch('/api/auth/session').then(r=>r.json()).then(async d=>{if(!d.user){setAllowed(false);return}const test=await fetch('/api/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})});setAllowed(test.status!==403)}).catch(()=>setAllowed(false));load()},[]);const change=(k:string,v:string)=>setForm(x=>({...x,[k]:v}));const upload=async(file?:File)=>{if(!file)return;setMessage('Завантаження фото...');const fd=new FormData();fd.set('image',file);const r=await fetch('/api/admin/upload',{method:'POST',body:fd});const d=await r.json();if(r.ok){change('image',d.url);setMessage('Фото додано.')}else setMessage(d.error||'Не вдалося завантажити фото');};const submit=async(e:FormEvent)=>{e.preventDefault();const payload={...form,price:Number(form.price)};const r=await fetch(editing?`/api/products/${editing}`:'/api/products',{method:editing?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(r.ok){setForm(blank);setEditing(null);setMessage(editing?'Товар оновлено.':'Товар додано.');load()}else{const d=await r.json();setMessage(d.error||'Помилка')}};const edit=(p:ManagedProduct)=>{setEditing(p.id);setForm({name:p.name,description:p.description,category:p.category,price:String(p.price),image:p.image});window.scrollTo({top:0,behavior:'smooth'})};const remove=async(id:string)=>{if(!confirm('Видалити цей товар?'))return;await fetch(`/api/products/${id}`,{method:'DELETE'});load()};if(allowed===null)return <main className="shell py-20"><div className="panel animate-pulse p-10">Перевіряємо доступ...</div></main>;if(!allowed)return <main className="shell grid min-h-[65vh] place-items-center py-14"><div className="panel max-w-lg p-9 text-center"><ShieldAlert className="mx-auto text-ember" size={38}/><h1 className="heading mt-5 text-4xl">Доступ заборонено</h1><p className="mt-4 text-stone-400">Ця панель доступна лише Steam ID зі списку <code>ADMIN_STEAM_IDS</code>.</p></div></main>;return <main className="shell max-w-6xl py-14"><p className="eyebrow">Обмежений доступ · Steam Admin</p><h1 className="heading mt-2 text-5xl">Керування магазином</h1><section className="panel cut mt-8 p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-[#29351d]"><Plus className="text-[#c4da83]"/></span><h2 className="text-xl font-bold">{editing?'Редагувати товар':'Додати товар'}</h2></div><form onSubmit={submit} className="mt-6 grid gap-4 md:grid-cols-2"><input required value={form.name} onChange={e=>change('name',e.target.value)} className="bg-black/30 p-3" placeholder="Назва товару"/><input required type="number" min="0" value={form.price} onChange={e=>change('price',e.target.value)} className="bg-black/30 p-3" placeholder="Ціна, ₴"/><input required value={form.category} onChange={e=>change('category',e.target.value)} className="bg-black/30 p-3" placeholder="Категорія"/><label className="flex cursor-pointer items-center gap-3 border border-dashed border-[#71834b] px-3 text-sm text-[#c4da83]"><Upload size={17}/> Завантажити фото<input onChange={e=>upload(e.target.files?.[0])} className="hidden" type="file" accept="image/*"/></label><textarea required value={form.description} onChange={e=>change('description',e.target.value)} className="min-h-28 bg-black/30 p-3 md:col-span-2" placeholder="Короткий опис"/>{form.image&&<div className="flex items-center gap-3 text-sm text-[#c4da83] md:col-span-2"><ImagePlus size={18}/> Фото вибрано: {form.image}</div>}<div className="flex gap-3 md:col-span-2"><button className="btn"><Save size={16}/>{editing?'Зберегти зміни':'Додати товар'}</button>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(blank)}} className="btn btn-outline">Скасувати</button>}</div></form>{message&&<p className="mt-4 text-sm text-[#c4da83]">{message}</p>}</section><section className="mt-10"><h2 className="heading text-3xl">Товари ({products.length})</h2>{!products.length?<div className="panel mt-4 p-10 text-center text-stone-400">Товарів ще немає. Додайте перший через форму вище.</div>:<div className="mt-4 grid gap-4 md:grid-cols-2">{products.map(p=><article key={p.id} className="panel flex gap-4 p-4"><img src={p.image} alt="" className="h-20 w-24 rounded object-cover"/><div className="min-w-0 flex-1"><p className="font-bold">{p.name}</p><p className="mt-1 truncate text-sm text-stone-500">{p.category} · {p.price} ₴</p><div className="mt-3 flex gap-2"><button onClick={()=>edit(p)} className="btn !min-h-8 !px-3"><Pencil size={14}/>Редагувати</button><button onClick={()=>remove(p.id)} className="btn btn-outline !min-h-8 !px-3 !border-ember !text-red-300"><Trash2 size={14}/></button></div></div></article>)}</div>}</section></main>}
