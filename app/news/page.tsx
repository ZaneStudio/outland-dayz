import {news} from "@/lib/data";export default function News(){return <main className="shell max-w-4xl py-14"><p className="eyebrow">Оперативні зведення</p><h1 className="heading mt-2 text-5xl">Новини</h1><div className="mt-8 space-y-4">{news.map(n=><article id={n.slug} key={n.slug} className="panel cut p-7"><p className="text-xs text-[#acbd75]">{n.date}</p><h2 className="mt-2 text-2xl font-bold">{n.title}</h2><p className="mt-4 leading-relaxed text-stone-400">{n.text} Слідкуйте за Discord, щоб не пропускати важливі оголошення та оновлення.</p></article>)}</div></main>}
