"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const r=useRouter(),[mode,setMode]=useState("login");return <main className="shell grid min-h-[70vh] place-items-center py-14"><form onSubmit={e=>{e.preventDefault();r.push('/profile')}} className="panel w-full max-w-md p-7"><p className="eyebrow">Ідентифікація</p><h1 className="heading mt-2 text-4xl">{mode==='login'?'Увійти':'Реєстрація'}</h1><input required className="mt-6 w-full bg-black/30 p-3" placeholder="Email або username"/><input required type="password" className="mt-3 w-full bg-black/30 p-3" placeholder="Пароль"/><button className="btn mt-5 w-full">{mode==='login'?'Увійти':'Створити акаунт'}</button><button type="button" onClick={()=>setMode(mode==='login'?'register':'login')} className="mt-4 text-sm text-[#b5c87a]">{mode==='login'?'Немає акаунта? Зареєструватися':'Вже є акаунт? Увійти'}</button></form></main>}
