"use client"; import {useMemo,useState} from "react"; import {products} from "@/lib/data"; import {ProductCard} from "@/components/product-card"; export default function Shop(){const [q,setQ]=useState(""),[cat,setCat]=useState("Усі"),[sort,setSort]=useState("popular"); const cats=["Усі",...new Set(products.map(p=>p.category))];const view=useMemo(()=>products.filter(p=>(cat==="Усі"||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==="price"?a.price-b.price:b.popular-a.popular),[q,cat,sort]);return <main className="shell py-14"><p className="eyebrow">Постачання зони</p><h1 className="heading mt-2 text-5xl">Магазин</h1><div className="mt-8 grid gap-3 md:grid-cols-[1fr_auto_auto]"><input value={q} onChange={e=>setQ(e.target.value)} className="border border-white/15 bg-black/30 px-4 py-3 outline-none focus:border-[#95a966]" placeholder="Пошук предмету..."/><select value={cat} onChange={e=>setCat(e.target.value)} className="border border-white/15 bg-[#121610] px-3">{cats.map(x=><option key={x}>{x}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)} className="border border-white/15 bg-[#121610] px-3"><option value="popular">За популярністю</option><option value="price">Від дешевших</option></select></div><p className="mt-6 text-sm text-stone-400">Знайдено: {view.length}</p><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{view.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!view.length&&<div className="panel mt-4 p-12 text-center text-stone-400">У цьому секторі поки немає предметів.</div>}</main>}
