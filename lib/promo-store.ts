import { randomBytes, randomUUID } from "crypto"; import { db } from "@/lib/db";
export type PromoCode={id:string;code:string;amount:number;maxActivations:number;activations:number;activatedSteamIds:string[];expiresAt:string;createdAt:string};
const map=(item:{id:string;code:string;amount:number;maxActivations:number;activations:number;expiresAt:string;createdAt:Date;uses:{steamId:string}[]}):PromoCode=>({...item,createdAt:item.createdAt.toISOString(),activatedSteamIds:item.uses.map(x=>x.steamId)});
export async function getPromoCodes(){return (await db.managedPromoCode.findMany({include:{uses:{select:{steamId:true}}},orderBy:{createdAt:"desc"}})).map(map);}
export async function createPromoCode(input:{amount:number;maxActivations:number;expiresAt:string}){return map(await db.managedPromoCode.create({data:{id:randomUUID(),code:`OUT-${randomBytes(4).toString("hex").toUpperCase()}`,...input},include:{uses:{select:{steamId:true}}}}));}
export async function activatePromoCode(code:string,steamId:string){return db.$transaction(async tx=>{const promo=await tx.managedPromoCode.findUnique({where:{code:code.toUpperCase().trim()},include:{uses:{where:{steamId}}}});if(!promo)return {error:"Промокод не знайдено"};if(new Date(`${promo.expiresAt}T23:59:59`).getTime()<Date.now())return {error:"Термін дії промокоду завершився"};if(promo.activations>=promo.maxActivations)return {error:"Ліміт активацій вичерпано"};if(promo.uses.length)return {error:"Ви вже використали цей промокод"};await tx.steamAccount.upsert({where:{steamId},update:{},create:{steamId}});await tx.promoUse.create({data:{promoId:promo.id,steamId}});await tx.managedPromoCode.update({where:{id:promo.id},data:{activations:{increment:1}}});return {amount:promo.amount,code:promo.code};});}
